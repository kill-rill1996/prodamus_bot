# TYPE  DATABASE  USER      ADDRESS             METHOD

# 1. Локальные подключения (через сокет Unix или с localhost)
#    Это нужно для приложений, запущенных непосредственно на сервере (не в Docker)
local   all       all                           md5
host    all       all       127.0.0.1/32        md5
host    all       all       ::1/128             md5

# 2. Подключения из контейнеров Docker (внутренняя сеть)
#    Замените 172.17.0.0/16 на реальную подсеть вашего Docker bridge
host    all       all       172.17.0.0/16       md5

# 3. Подключения с вашего удаленного компьютера (внешний IP)
host    all       all       95.24.177.102/32    md5

# 4. Безопасность: запретить все остальные внешние подключения
host    all       all       0.0.0.0/0           reject
host    all       all       ::/0                reject